<p>
给定一个长度为 n 的整数数组 a，以及一个区间 [l, r]。
如果某个 <strong>连续子数组</strong> 的元素和满足：
<br><strong>l ≤ 子数组元素和 ≤ r</strong>，
则称该连续子数组是一个「强大的子数组」。
</p>
<p>
现在允许从原数组中选取 <strong>若干个互不重叠的连续子数组</strong>，
且不要求覆盖所有元素。
目标是：<strong>最多能选出多少个强大的连续子数组</strong>。
</p>
